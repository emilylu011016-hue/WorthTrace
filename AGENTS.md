# WorthTrace 项目协作说明

## 沟通方式

- 默认使用中文回答。
- 涉及代码时，先用大白话说明修改目的和影响，再给出具体操作或代码。
- 修改文件、运行命令、访问外部账号前，先说明将做什么及风险。
- 明确区分：
  - **现有资料**：从代码、配置、文档或命令结果直接确认的事实。
  - **推断/建议**：基于现有资料得出的判断、方案或尚未验证的设想。
- 信息不足时先检查项目文件；高风险或会改变产品行为的歧义，必须向用户确认。

## 项目概览

WorthTrace（钱迹）是本地优先的个人财务记录与分析应用。

- 桌面端：Tauri 2 + React 18 + TypeScript + Rust + SQLite。
- 手机端：`mobile/pwa/` 下的静态 PWA。
- 核心财务计算：`packages/finance-core/`。
- 正式本地 SQLite 数据库是完整财务数据的唯一事实来源。
- Supabase 只承载账号、手机草稿和看板快照，不是完整正式数据库。

## 目录与事实来源

- `src/`：桌面前端和交互。
- `src/styles/`：桌面端全局样式和主题。
- `src-tauri/src/`：Tauri/Rust 后端、SQLite 读写、同步和本地服务。
- `src-tauri/migrations/`：数据库初始化与迁移。
- `packages/finance-core/`：储蓄率、资产配置、XIRR 等核心计算。
- `mobile/pwa/`：手机 PWA 正式源码。
- `mobile/mockups/`：视觉和交互方向稿，不连接正式数据。
- `scripts/`：启动、演示、导入、导出和数据库检查脚本。
- `docs/formulas.md`：财务公式定义。
- `docs/data_schema.md`：数据表语义和数据规则。
- `docs/final_architecture.md`：数据边界和主要业务流程。
- `docs/platforms_and_accounts.md`：GitHub、Vercel、Supabase、Resend 和本地数据库的关系。
- `docs/version_workflow.md`：正式、测试、Demo 环境及发布流程。
- `brand/qianji-brand-spec.md`：品牌视觉规范。

若文档与运行代码不一致，先报告差异，不擅自选择一方；确认后同步修正文档和代码。

## Git 分支与发布流程

- `dev` 是默认开发分支。开始任何代码或文档修改前，先确认当前分支是 `dev`。
- `main` 是正式稳定分支。日常开发、测试和未发布修改不得直接写入 `main`。
- 若当前不在 `dev`，除非任务是只读检查、明确的发布操作或用户指定的其他分支，否则先停止修改并报告当前分支。
- 用户说“提交”时，只在 `dev` 提交当前已确认的修改，不更新 `main`。
- 只有用户明确说“提交发布”“发布正式版”或“更新 main”时，才能在验证通过后把 `dev` 更新到 `main`；发布结束后回到 `dev`。
- 合并、打 tag、推送 GitHub、构建正式安装包、部署 Vercel 或访问其他外部平台，仍需按用户明确指定的发布范围执行，不自动扩大操作。
- 新 worktree 的日常开发默认从最新 `dev` 创建；只有明确的正式版热修复任务才从 `main` 创建。
- 不在多个 worktree 中同时修改同一功能或同一批文件；完成后先检查 diff 和验证结果，再决定 Hand off、合并或丢弃。

## 手机跨网络与离线使用 SOP

- 手机正式入口必须使用公网 HTTPS PWA：`https://worth-trace.vercel.app`。
- 电脑 App 里的“复制手机链接”也必须生成公网 HTTPS 地址；不得把 `192.168.x.x:18742` 局域网地址作为正式默认入口。
- 局域网链接只用于电脑开着时的兼容调试，不满足手机跨 Wi-Fi、流量访问或电脑关闭后的使用要求。
- 手机首次使用必须在联网状态打开公网入口、登录同一云端账号，并可添加到主屏幕。
- 手机断网后的收入、支出和允许的草稿操作写入浏览器 IndexedDB；恢复联网后上传 Supabase 云端草稿。
- 电脑端重新打开后，用同一账号拉取云端草稿；完整正式财务数据仍只保存在本地 SQLite。
- 修改手机 PWA 或桌面端手机链接后，必须同步更新 PWA 版本、Service Worker 缓存版本、Manifest、Rust 内嵌版本和线上 Vercel 部署。
- 验收必须覆盖：手机流量、不同 Wi-Fi、电脑 App 关闭、手机断网打开、离线新增记录、恢复联网后同步，以及电脑端拉取。

## 数据安全红线

- 未经用户明确确认，不得读取、复制、修改或删除正式数据库：
  `~/Library/Application Support/com.emilylu.financial-planning/financial_planning.sqlite3`。
- 未经明确确认，不得启动 `scripts/start_latest.command`；它面向正式源码环境，可能连接正式数据库。
- 开发和验收优先使用测试环境；演示优先使用脱敏 Demo 环境。操作前必须确认目标数据库的绝对路径和环境类型。
- 数据库写入、导入、重置、迁移或批量修复前，说明影响范围、备份方式和恢复方式，并取得确认。
- 不修改导入的原始 Numbers/CSV；`raw_transactions` 原始记录保持不变，用户修订进入确认层。
- 不把正式库、测试库、Demo 库、备份、导出财务数据、访问令牌或私钥提交到 Git。
- 日志、截图、测试夹具和错误信息不得泄露真实财务数据、邮箱、令牌或其他隐私信息。
- 已应用的迁移文件原则上不改写；数据库结构变化应新增迁移，并同步数据模型、查询和文档。
- 禁止在未确认目标时执行清库、覆盖、重置、递归删除或不可逆操作。

## 修改原则

- 开始前检查 `git status`，保留用户已有修改；不覆盖、不回滚无关改动。
- 只做完成任务所需的最小改动，不顺手重构无关代码。
- 财务公式修改必须以 `docs/formulas.md` 和 `packages/finance-core/` 为核心核对点，并检查前端展示、Rust 计算和文档是否一致。
- 金额、汇率、百分比、日期、月份边界和舍入规则必须明确；不得用未经确认的默认值掩盖缺失数据。
- 修改月末流程时，保持步骤状态、保存成功条件、关账条件和报表读取口径一致。
- 修改云同步时，保持用户隔离、RLS 边界、草稿/正式数据分层；不得把完整正式 SQLite 数据上传云端。
- 修改手机 PWA 逻辑或静态资源时，检查缓存版本、Service Worker、manifest 和 `mobile/README.md` 的版本说明。
- 修改品牌或 UI 时，优先沿用现有香槟、鼠尾草、石墨主题及 `brand/` 规范。
- 版本号、Git tag、正式安装包和线上发布只在用户明确要求发布时更新。

## 常用验证命令

运行前仍需说明命令用途和风险。

```bash
# 桌面前端 TypeScript 检查与生产构建
npm run build

# Rust/Tauri 后端静态检查
cargo check --manifest-path src-tauri/Cargo.toml

# 手机 PWA 构建；会重建可再生目录 dist-mobile/
npm run build:mobile
```

当前 `package.json` 没有独立的 `test` 或 `lint` 脚本，不得声称已运行不存在的测试。若任务涉及数据库脚本，先查看脚本参数和目标库，再决定验证命令。

## 完成标准

- 对受影响范围运行可用且相关的构建/检查。
- 财务逻辑变更需用边界案例核对：零收入、负值、多币种、跨月、信用卡结转、空数据和舍入。
- 数据库变更需验证新建库和已有库迁移路径；不得用正式库做实验。
- 检查最终 diff，确认没有混入用户既有改动、生成物、数据库、凭据或真实财务数据。
- 清楚报告：改了什么、验证了什么、哪些未验证及原因、剩余风险。
- 除非用户明确要求，不执行提交、推送、合并、打 tag、部署、发布或访问外部平台账号。
